{
  "name": "ZubairHub Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye",
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python",
        "python.linting.enabled": true
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "pip install --upgrade pip uv && [ -f packages.txt ] && sudo apt update && sudo apt install -y $(cat packages.txt) && uv pip install -r requirements.txt",
  "postAttachCommand": {
    "init": "echo '✅ Dev Container Ready! Use `uv pip install ...` to add packages.'"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}